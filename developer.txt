DEVELOPER FILE - SAFETY DETECTION SYSTEM

PROJECT IDEOLOGY

Problem Statement
Workers in construction sites must wear safety equipment like hard hats, safety vests, gloves, and safety glasses. Manual monitoring by supervisors is difficult and time consuming. This leads to safety violations going unnoticed.

Solution Approach
Develop an automated system that uses computer vision and artificial intelligence to continuously monitor workers and detect if they are wearing required safety equipment. The system alerts supervisors immediately when violations are detected.

Core Purpose
Improve workplace safety through automated continuous monitoring, reduce accidents by ensuring compliance, and create safer working environments.

METHODOLOGY

Development Approach
Started with basic prototype to understand requirements, then developed functional system with core features, and finally added advanced features for complete solution.

Implementation Steps
Phase 1: Created basic camera capture and frame processing system
Phase 2: Integrated AI model for actual PPE detection
Phase 3: Added data storage, user interface, and reporting features
Phase 4: Implemented authentication, statistics, and export functionality

Testing Method
Tested camera connectivity and frame capture, validated AI model accuracy with sample images, verified all features work correctly, and tested web interface functionality.

WORKING PRINCIPLES

Basic Flow
Camera captures live video feed from webcam, each frame is extracted and preprocessed for AI model, processed frame is sent to trained neural network, model predicts which safety items are present or missing, system analyzes predictions to determine compliance status, results are displayed on screen and saved to storage.

Detection Process
Frame is resized to 224x224 pixels, image colors are converted from BGR to RGB format, pixel values are normalized between 0 and 1, preprocessed image is fed to AI model, model returns confidence scores for each safety item, scores above threshold indicate item is detected.

Compliance Checking
System checks if all four required items are detected with sufficient confidence, if all items present, status is marked as compliant, if any item missing, status is marked as violation, violation triggers alert sound and screenshot saving.

Data Management
Each detection result is saved with timestamp to JSON file, entry and exit times are tracked automatically based on compliance, statistics are calculated from stored data, data can be exported to CSV for analysis.

ALGORITHM USED

Image Classification Algorithm
Uses convolutional neural network CNN for image classification, CNN processes images in layers to identify patterns, lower layers detect basic features like edges and shapes, higher layers detect complex features like safety equipment, final layer outputs probability scores for each class.

Transfer Learning
Uses pre-trained EfficientNet model as base, pre-trained model already knows how to recognize objects in images, fine-tuned last few layers for PPE detection, this approach requires less training data and works better.

Classification Process
Input image is processed through multiple convolutional layers, each layer extracts more complex features, final fully connected layer maps features to classes, softmax function converts outputs to probabilities, highest probability class is the prediction.

TECHNOLOGIES USED

Computer Vision Technology
OpenCV library for camera access and image processing, handles video capture from webcam, processes frames in real time, provides image manipulation functions.

Artificial Intelligence Technology
TensorFlow framework for deep learning, provides pre-trained models and neural network building blocks, handles model training and inference, optimized for fast predictions.

Web Application Technology
Flask framework for creating web interface, handles HTTP requests and responses, serves HTML pages and data, manages user sessions and routing.

Data Storage Technology
JSON file format for storing detection history, simple text-based storage, no complex database needed, easy to read and write.

LANGUAGES USED

Python
Primary programming language used throughout the project, simple and easy to learn syntax, extensive library support for AI and computer vision, widely used in machine learning projects, good community support and documentation.

JavaScript
Used for web interface interactivity, handles real-time updates on webpage, communicates with backend server using AJAX, manages user interface elements dynamically.

HTML
Creates structure of web pages, defines layout and content, used for login page and main dashboard, simple markup language easy to understand.

CSS
Styles the web interface, controls colors, fonts, and layout, makes interface visually appealing, separated from HTML for better organization.

LIBRARIES USED

OpenCV
Purpose: Computer vision and image processing
Why Used: Industry standard for image and video processing, easy to use camera functions, fast frame processing, good documentation
How Used: Captures video frames from webcam, preprocesses images, handles image operations

TensorFlow
Purpose: Deep learning and AI model
Why Used: Most popular deep learning framework, supports pre-trained models, fast inference speed, good for production use
How Used: Loads trained PPE detection model, performs predictions on images, handles neural network operations

Flask
Purpose: Web framework
Why Used: Lightweight and simple, easy to learn, perfect for small to medium applications, minimal setup required
How Used: Creates web server, defines routes for different pages, handles user requests, serves HTML templates

NumPy
Purpose: Numerical computations
Why Used: Fast array operations, required by TensorFlow and OpenCV, handles image data efficiently
How Used: Processes image arrays, performs mathematical operations, converts data formats

JSON
Purpose: Data storage
Why Used: Built into Python, human readable format, easy to parse and write, no external dependencies
How Used: Saves detection history to file, loads data on startup, exports data in readable format

Batch Files
Purpose: Easy system startup
What Are Batch Files: Batch files are simple text files with .bat extension that contain commands, when you double click a batch file, Windows automatically runs all commands inside it, think of it as a list of instructions that computer follows automatically, they are used to automate repetitive tasks

How Batch Files Work: Batch files contain commands written in plain text, each line is a command that would normally be typed in command prompt, Windows Command Processor reads these commands one by one, executes them in sequence automatically, no need to type commands manually

Why Used in This Project: Provides easy way to start the system without technical knowledge, users don't need to open command prompt, users don't need to remember Python commands, just double click and system starts, makes project more user-friendly, especially important for non-technical users like supervisors or managers

How Batch Files Are Used: Our start.bat file contains simple command: python run.py, when user double clicks start.bat, it automatically opens command prompt, runs the python run.py command, starts our safety detection system, user sees output and system starts working

What's Inside Our Batch File: First line sets window title to Safety Detection System, second line displays message Starting Safety Detection System, third line runs the command python run.py which starts our application, last line keeps window open if there's an error

Benefits of Using Batch Files: Simplifies user experience, reduces chance of typing errors, makes project look more professional, easier to distribute and use, can be used by anyone regardless of technical skills

Creating and Editing Batch Files: Can be created using simple Notepad, just type commands and save with .bat extension, very easy to modify if needed, no special software required, anyone can create or edit batch files

Example Usage: User receives project folder, sees start.bat file, double clicks it, system starts automatically, no need to learn command prompt, no need to remember Python commands, works for everyone

WHY THIS TECHNOLOGY OR LIBRARIES WERE USED

Why Python
Easy to learn and use, excellent for beginners, huge collection of libraries for AI and computer vision, active community support, widely used in industry

Why OpenCV
Standard library for computer vision tasks, simple camera access, fast image processing, well documented with examples

Why TensorFlow
Powerful framework for deep learning, supports transfer learning with pre-trained models, optimized for performance, industry standard

Why Flask
Simple and lightweight framework, easy to understand for beginners, minimal configuration needed, perfect for small projects

Why JSON Storage
No database setup required, simple file-based storage, easy to backup and transfer, human readable format

HOW TO USE

Installation Steps
Install Python 3.8 or higher from python.org, open command prompt in project folder, run command: pip install -r requirements.txt, wait for all libraries to install

Running the System

Method 1: Using Batch File (Easiest)
Double click start.bat file in project folder, command prompt window will open automatically, Python program will start running, browser will open automatically showing the interface, if browser doesn't open, manually go to http://localhost:5000

Method 2: Using Command Prompt
Open command prompt, navigate to project folder using cd command, type python run.py and press Enter, system will start and browser will open

Why Batch File is Better
Batch file method is easier for non-technical users, no need to open command prompt manually, no need to remember commands, just double click and everything works, preferred method for most users

Batch File Explanation

What is start.bat
start.bat is a Windows batch file that helps start the system easily. Batch files are simple script files that run commands automatically. When you double click start.bat, it automatically runs the python run.py command for you.

Why We Created It
Normally to run Python programs, users need to open command prompt, navigate to project folder, type python run.py command. This requires technical knowledge. With batch file, user just double clicks and system starts. Makes project accessible to everyone.

How It Works
When you double click start.bat file, Windows automatically opens command prompt window, executes the commands written inside batch file, runs our Python program, displays any messages or errors. Everything happens automatically.

What Commands Are Inside
Our start.bat file contains simple commands: title command sets window title, echo command displays messages, python run.py runs our main program. All these commands are executed automatically when batch file is opened.

User Experience
Without batch file: User must open command prompt, change directory, type python run.py, remember exact syntax. With batch file: User just double clicks start.bat, everything happens automatically, no technical knowledge needed, suitable for all users.

Technical Details
Batch files have .bat extension, they are plain text files, can be edited with Notepad, contain DOS or Windows commands, execute commands in sequence from top to bottom, very simple and powerful automation tool.

Alternative Methods
System can also be started by opening command prompt and typing python run.py. But batch file makes it much easier. Users can create shortcut to batch file on desktop for even easier access. Batch file is optional but recommended for ease of use.

Login Process
Enter username and password on login page, default username: admin, default password: admin123, click Login button

Starting Detection
Click Start Detection button on main page, position yourself in front of camera, system will start analyzing frames, results appear on screen in real time

Viewing Information
Check Today's Entry Information for in and out times, view Statistics for compliance rates, see Daily Entries table for all detections today

Exporting Data
Click Export CSV button, file will download automatically, open in Excel or any spreadsheet software, analyze detection data

Stopping Detection
Click Stop Detection button, camera will stop processing, system continues to run in background

Logout
Click Logout button in header, returns to login page, session is cleared

PROJECT FUTURE SCOPE

Additional Safety Items
Add detection for safety boots, add detection for safety harnesses, add detection for reflective clothing, customize required items per site

Multiple Cameras
Support for multiple cameras simultaneously, monitor different areas of site, central dashboard for all cameras, camera selection interface

Advanced Analytics
Generate detailed compliance reports, create visual charts and graphs, track trends over time, identify peak violation times, worker-specific statistics

Mobile Application
Create mobile app for supervisors, receive push notifications on violations, view live camera feed on phone, access statistics on the go

Integration Features
Integrate with existing safety management systems, send alerts to management software, connect with attendance systems, export to multiple formats

Face Recognition
Identify individual workers, track compliance per worker, generate worker-specific reports, link violations to specific employees

Real-time Alerts
Email notifications for violations, SMS alerts for critical violations, integrate with communication systems, alert escalation for repeated violations

PROJECT SUMMARY

What This Project Does
Automatically monitors workers using camera feed, detects if safety equipment is being worn, alerts when violations occur, saves evidence as screenshots, tracks entry and exit times, generates statistics and reports

Key Features
Real-time PPE detection using AI, automatic violation screenshot saving, daily entry and exit time tracking, compliance statistics and reporting, data export to CSV format, simple web-based interface, user authentication system

Technical Implementation
Uses computer vision for image processing, employs deep learning for object detection, stores data in JSON files, provides web interface using Flask, implements sound alerts for violations

Benefits
Continuous automated monitoring replaces manual checking, immediate alerts prevent accidents, documented evidence through screenshots, historical data for analysis, improved safety compliance, reduced supervision workload

Technologies Summary
Python for programming, TensorFlow for AI, OpenCV for computer vision, Flask for web interface, JSON for data storage, HTML CSS JavaScript for frontend, Batch files for easy system startup

Project Outcome
A functional safety monitoring system that can detect PPE compliance in real time, provides alerts and notifications, stores historical data, generates reports, and helps improve workplace safety through automation

